{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://semantic-engine.dev/contracts/protocol-request.schema.json",
  "title": "Semantic Engine Protocol Request",
  "description": "One request line accepted by the version 2 JSONL transport.",
  "type": "object",
  "unevaluatedProperties": false,
  "required": ["protocol_version", "request_id", "command"],
  "properties": {
    "protocol_version": { "const": 2 },
    "request_id": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[A-Za-z0-9._:-]+$" },
    "command": { "enum": ["start_session", "get_session", "submit", "resolve", "remember_resolution", "list_memory", "revoke_memory", "end_session", "events", "stats"] },
    "params": {}
  },
  "oneOf": [
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "start_session" },
        "params": { "$ref": "session-start.schema.json" }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "get_session" },
        "params": { "$ref": "#/$defs/session_identity" }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "submit" },
        "params": {
          "type": "object",
          "additionalProperties": false,
          "required": ["session_id", "submission"],
          "properties": {
            "session_id": { "$ref": "#/$defs/session_id" },
            "submission": { "$ref": "submission.schema.json" }
          }
        }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "resolve" },
        "params": {
          "type": "object",
          "additionalProperties": false,
          "required": ["session_id", "request"],
          "properties": {
            "session_id": { "$ref": "#/$defs/session_id" },
            "request": { "$ref": "operator-resolution-request.schema.json" }
          }
        }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "end_session" },
        "params": { "$ref": "#/$defs/session_identity" }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "remember_resolution" },
        "params": {
          "type": "object",
          "additionalProperties": false,
          "required": ["session_id", "message_id"],
          "properties": {
            "session_id": { "$ref": "#/$defs/session_id" },
            "message_id": { "$ref": "#/$defs/session_id" }
          }
        }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "list_memory" },
        "params": {
          "type": "object",
          "additionalProperties": false,
          "required": ["context_package_sha256"],
          "properties": {
            "context_package_sha256": { "type": "string", "pattern": "^[A-Fa-f0-9]{64}$" },
            "limit": { "type": "integer", "minimum": 0, "maximum": 1000 },
            "active_only": { "type": "boolean", "default": false }
          }
        }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "revoke_memory" },
        "params": {
          "type": "object",
          "additionalProperties": false,
          "required": ["context_package_sha256", "id"],
          "properties": {
            "context_package_sha256": { "type": "string", "pattern": "^[A-Fa-f0-9]{64}$" },
            "id": { "type": "string", "pattern": "^[a-f0-9]{32}$" }
          }
        }
      }
    },
    {
      "required": ["params"],
      "properties": {
        "command": { "const": "events" },
        "params": {
          "type": "object",
          "additionalProperties": false,
          "required": ["session_id"],
          "properties": {
            "session_id": { "$ref": "#/$defs/session_id" },
            "after_sequence": { "type": "integer", "minimum": 0 },
            "limit": { "type": "integer", "minimum": 0, "maximum": 1000 }
          }
        }
      }
    },
    {
      "properties": { "command": { "const": "stats" } },
      "not": { "required": ["params"] }
    }
  ],
  "$defs": {
    "session_id": { "type": "string", "minLength": 1, "maxLength": 256, "pattern": "^[A-Za-z0-9._:-]+$" },
    "session_identity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["session_id"],
      "properties": { "session_id": { "$ref": "#/$defs/session_id" } }
    }
  }
}
