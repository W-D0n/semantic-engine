{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the local validation console",
  "windows": ["main"],
  "permissions": ["core:default"]
}
